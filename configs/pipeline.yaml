pipeline:
  client_download_root: data/external/client_downloads
  teacher_score_dir: data/external/client_downloads/teacher_scores
  client_task_dir: Client/outputs
  raw_audio_dir: data/raw/audio
  processed_mfcc_dir: data/processed/mfcc
  label_dir: data/raw/labels_teacher
  manifest_dir: data/processed/manifests
  dpo_dir: data/processed/dpo
  frontend_export_dir: outputs/frontend

score:
  dimensions:
    - Vibrato
    - Throat
    - Position
    - Open
    - Clean
    - Resonate
    - Unify
    - Falsetto
    - Chest
    - Nasal
  score_dims: 10
